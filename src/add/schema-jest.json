{
  "$schema": "http://json-schema.org/schema",
  "id": "SchematicsJestInstall",
  "title": "Jest Install Options Schema",
  "type": "object",
  "properties": {
    "project": {
      "type": "string",
      "description": "The name of the project.",
      "$default": {
        "$source": "projectName"
      }
    }
  },
  "required": []
}
